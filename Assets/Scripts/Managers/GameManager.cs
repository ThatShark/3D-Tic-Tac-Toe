using UnityEngine;

public class GameManager : MonoBehaviour {
    public int nowRound = 0;
    void Start() {
        nowRound = 1;
    }

    // Update is called once per frame
    void Update() {

    }
}
